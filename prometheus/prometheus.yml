global:
  scrape_interval: 5s

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]

rule_files:
  - "alert_rules.yml"  # 🔥 alert 조건 정의

scrape_configs:
  - job_name: "inference-api"
    static_configs:
      - targets: ["mlflow:8000"]
